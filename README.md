# nix-flakes

## 메모
- ~/.vimrc, ~/.ideavimrc
  ```
  set sol
  set nu
  set rnu
  set cul
  set so=4
  set hls
  set is
  set ic
  set scs
  set spr
  set sb
  set ai
  ```
- apple developer tools
- k8s contexts
- ssh key
- apps from store (snap, be focused, wireguard, kakaotalk)
- sable, montag
- karabiner(?)
- macvim
